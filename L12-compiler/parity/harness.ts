import { ParityMode } from "./parity-mode";

export function runParityHarness(mode: ParityMode, morphismId: string, input: unknown): never {
  void mode;
  void morphismId;
  void input;
  throw new Error("NOT_IMPLEMENTED: runParityHarness");
}
